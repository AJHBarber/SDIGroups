#pragma once
#include <vector>;
#include <string>;

using namespace std;


struct Crew{

	string producer, director, writer, editor, productionDesigner, setDecorator, costumeDesigner;
	vector<string>actors;

};